process {
    withName: '.*SNAPATAC2_QUALITYCONTROL' {
        ext.filtered = params.atac.filtered
        ext.args      = {
            [   
                "--amulet-qthresh ${params.amulet.q_thresh}",
                "--min-fragments ${params.atac.min_num_fragments}",
                "--max-mito-frac ${params.atac.max_mito_frac}",
                "--mad-thresh ${params.atac.mad_thresh}",
                params.atac.mad_keys instanceof List ? "--mad-key ${params.atac.mad_keys.join(' --mad-key ')}" : "",
            ].join(' ')
        }
        cpus       = 1
        queue      = "normal"
        memory     = { fileSizeInGB(h5ad) + 4.GB + 16.GB * (task.attempt - 1) }
        publishDir = [
            mode     : 'link',
            path     : { "${params.outdir}/h5ad/filtered/" },
            overwrite: true
        ]
    }
}